# Codex Native Maintainer E2E

This documentation-only fixture verifies that a project-scoped Codex Scheduled
Task discovers a labeled pull request, includes it in a batch review, and asks
for native user approval before any GitHub write operation.

It does not change the codexU application, build, release, or runtime behavior.
